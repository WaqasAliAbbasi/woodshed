# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# server's SQLite database (default DB_PATH, see server/env.ts) and its
# WAL/SHM sidecar files — created on first boot, never committed. Same
# reasoning as why the old IndexedDB data never lived in this repo either.
/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
